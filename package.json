{
  "name": "timetable-for-tv-app",
  "private": true,
  "scripts": {
    "dev": "npm run dev:frontend",
    "dev:frontend": "cd frontend && npm run dev",
    "test": "cd frontend && npm test && cd src-tauri && cargo test && cd ../.. && cd admin-panel && npm test",
    "test:frontend": "cd frontend && npm test && cd src-tauri && cargo test",
    "test:admin": "cd admin-panel && npm test"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
